########################
####  Rule Targets  ####

################  rule  ################
genrule(
    name  = "__config.mlh__",
    srcs  = [
        "//rules/copy/config:config.mlh",
        "//rules/copy/foo:bar.x",
        "baz.y"
    ],
    outs  = [
        "config.mlh"
    ],
    cmd   = " ".join([
        "$(location copy)",
        "$(location rules/copy/config/config.mlh)",
        "$(location rules/copy/config/config.mlh)"
    ]),
    tools = [
        "copy"
    ]
)
