package(default_visibility = ["//visibility:public"])

############################# Rules ##################################

########
genrule(
    outs  = [
        "output.txt"
    ],
    name  = "__output.txt__",
    cmd   = " ".join([
        "$(execpath //#f:bar)",
        "$(location output.txt)",
    ]),
    exec_tools = [
        "//#f:bar"
    ]
)

